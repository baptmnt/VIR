# Requiert d'avoir répété les étapes d'install de Traefik si sur la clé
# Notamment, il faut que la gateway soit sur le namespace par défaut. (kubectl get gateway)
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ include "minecraft.fullname" . }}-route
spec:
  parentRefs:
    - name: traefik-gateway
  hostnames:
    - {{ .Values.route.hostname }}
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: {{ include "minecraft.fullname" . }}-websvc
          port: 80
 